# ACF Form Engine Core PRO (v1.3.2)
- Composer autoload PSR-4 incluido (run: `composer dump-autoload -o`).
- Bloque ACF `acf/fecp-form` (contacto / cotizacion), JSON load-only (no duplicados).
- JS validation en tiempo real (animada, mensajes bajo input, borde rojo/verde).
- REST nonce + DB prepare + CRUD admin con nonce.
- Token por `ACF_FE_API_TOKEN` (wp-config o env).
