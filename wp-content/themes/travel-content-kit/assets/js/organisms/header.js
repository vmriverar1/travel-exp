/**
 * Header - Static (non-sticky)
 * No JavaScript behavior needed for static header
 */

(function() {
    'use strict';

    // Header is now static and doesn't require JavaScript behavior
    // If you need to add functionality in the future, do it here

})();
